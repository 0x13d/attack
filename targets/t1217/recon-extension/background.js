// BENIGN demo target — T1217. Does nothing: it only *holds* bookmarks/history/topSites permissions so the
// attack EDR flags it as recon-capable. It never reads or exfiltrates anything.
console.log('[T1217 demo target] loaded — holds recon permissions but does nothing.');
