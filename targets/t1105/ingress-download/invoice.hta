<!--
  BENIGN DEMO ARTIFACT — attack / T1105 Ingress Tool Transfer.
  This file is plain HTML/text. It does nothing. It exists only so the EDR's downloads rule sees a
  rarely-legitimate ".hta" artifact arriving and cancels it. Safe to open, safe to delete.
-->
<!doctype html>
<meta charset="utf-8">
<title>benign demo artifact</title>
<p>This is a harmless demo file used to exercise the attack EDR's T1105 download rule. It is not a real HTA application and performs no actions.</p>
